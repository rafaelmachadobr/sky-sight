version: "3.8"

services:
  backend:
    container_name: sky-sight-backend
    build: ./backend
    volumes:
      - ./backend/:/usr/src/app/
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.dev
